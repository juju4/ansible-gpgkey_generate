---

- hosts: all
  roles:
#    - gpgkey_generate
    - { role: gpgkey_generate, gpg_user: dupont, gpg_realname: 'Dupont User', gpg_useremail: 'dupont@localhost', gpg_pubkeyfile: 'dupont.pub', gpg_privkeyfile: 'dupont.priv' }

